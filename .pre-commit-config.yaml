repos:
  - repo: local
    hooks:
      - id: no-op
        name: silence-pre-commit
        entry: echo "pre-commit hooks disabled for GrokDream"
        language: system
        always_run: true
        pass_filenames: false
