# For additional machines: copy the entire grokforge folder + venv
REDIS_HOST=localhost          # change to primary machine IP on secondary machines
REDIS_PORT=6379
PALACE_PATH=/home/Irikash/grokforge-palaces/default_palace   # use NFS/shared mount for multi-machine palace
